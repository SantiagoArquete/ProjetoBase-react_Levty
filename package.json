{
  "name": "exemplo-aula",
  "version": "1.0.0",
  "description": "Script para rodar backend e frontend juntos",
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
